import AuthCard from "@/components/auth/auth-card";

export const metadata = {
  title: "Create account | Strategy Hub",
};

export default function RegisterPage() {
  return <AuthCard mode="register" />;
}
